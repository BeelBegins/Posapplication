# Client runtime

Load the matching local skill before editing. ERP/aimatic remains authoritative
for identity, permissions, prices, final totals, stock, accounting, FBR,
loyalty and vouchers. Keep Electron/Capacitor code separated behind existing
bridges and product profiles. Validate boundary inputs and degraded/offline
states. Never add secrets or a generic API escape hatch.

Changes to sale/refund, payment, shift, queue/idempotency, receipt, auth,
pricing or product isolation require regression checks and server alignment.
